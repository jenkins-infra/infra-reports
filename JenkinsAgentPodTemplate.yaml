apiVersion: v1
kind: Pod
spec:
  containers:
    - image: "jenkinsciinfra/builder:2.0.57"
      imagePullPolicy: "IfNotPresent"
      name: "jnlp"
      resources:
        limits:
          memory: "256Mi"
          cpu: "1"
        requests:
          memory: "256Mi"
          cpu: "1"
      securityContext:
        privileged: false
